<section>
  <%= render @profile %>
</section>

<section>
  <% @profile.user.articles.each do |article| %>
    <div class="m-10 p-5 border border-gray-400 rounded-lg">
      <span class="font-bold text-lg">
        <%= article.name %> <br>
      </span>
      <span class="font-semibold text-sm">
        <%= article.description %>
      </span>
    </div>
  <% end %>
</section>